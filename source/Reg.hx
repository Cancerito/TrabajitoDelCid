package;
import flixel.group.FlxGroup;

/*
 lo que pasa cuando hacemos las balas con este array, que esta basio ahora pero disponible para ponerle balas,
 es que la pantalla "no lo ve" entonces no podira hacer las coliciones porque en teoria no lo ve
 
 */
class Reg
{

	static public var grupoBalasEnemigos:FlxGroup; // esto hace que el array sea global y pueda ser haccedido desde el main
	
}